//  Calculate the Area of the Triangle

let baseValue = prompt("enter the baseValue of the triangle");
let Height = prompt("enter the value of height");

let AREA = (baseValue * Height) / 2;
console.log(AREA);
