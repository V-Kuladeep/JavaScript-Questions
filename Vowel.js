// To find the count of vowels

function countVowel(str) {
    let count = str.match(/[aeiou]/gi).length
    return count;
}
let tring = promt("Enter the letter");
let result = countVowel(string);
console.log(result);<div className=""></div>